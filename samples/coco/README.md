# Representative COCO annotation

`example-annotation.json` demonstrates a small public interchange shape: one image, three categories and two annotations. Pixel boxes use `[x, y, width, height]`.

The coordinates are illustrative. This file is intentionally too small to be a dataset, contains no internal fields and does not promise that a private production manifest uses the same structure.
