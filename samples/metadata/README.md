# Representative public frame metadata

`example-frame.public.json` contains only fields useful for public inspection: dataset identity, frame identity, image dimensions, engine/map provenance and modality names.

Local paths, machine names, internal actor references, private configuration and implementation-specific fields have been removed. Treat the example as documentation, not as a complete production schema.
